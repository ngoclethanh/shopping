export enum NotifyMessageType {
  Success = 'success',
  Error = 'error',
  Info = 'info',
  Warn = 'warn',
}

export enum SessionKey {
  UserProfile = 'KEY_USER_PROFILE',
}

export enum FunctionCode {
  Employee = 'EMPLOYEE',
  Branch = 'BRANCH',
}

export enum CommonCategoryCode {
  Title = 'TILE',
  Gender = 'GENDER',
}

export enum ScreenType {
  Create,
  Update,
  Detail,
}
export enum FieldType {
  Text,
  TextArea,
  Date,
  // Dropdown,
  // Checkbox,
  // RadioButton,
}
